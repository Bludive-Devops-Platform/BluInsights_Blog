apiVersion: v1
kind: Service
metadata:
  name: {{ include "comment-service.fullname" . }}
  labels:
    app: {{ include "comment-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "comment-service.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}

